   {% set url = path('fichevroom',{'slug':car.slug }) %}
         

				<div class="col-md-4 mb-2 " style="height:550px";>
					<div class="card border-warning">
						<div class="card-header">{{ car.marque }}
							<strong class="text-warning">{{ car.modele }}</strong>
						</div>
						
						<img src="/uploads/{{car.imgCover}}" class="img-fluid" alt="{{ car.modele }}" style="230px;">
						<div class="card-body">
							<h4 class="card-title">{{ car.modele }}</h4>
							<p class="card-text text-truncate">{{ car.description }}</p>
							<a href="{{ url }}" class="btn btn-danger col-6 mb-1">Voir plus ...</a>
                               {% if (app.user and app.user == car.author) or is_granted('ROLE_ADMIN') %}
                                <a href="{{ path('edit',{'slug': car.slug})}}" class="btn btn-secondary">Modification de l'annonce</a>
                          {% endif %}
						</div>
					</div>
				</div>

				